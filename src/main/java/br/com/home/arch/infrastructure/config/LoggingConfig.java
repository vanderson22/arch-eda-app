package br.com.home.arch.infrastructure.config;

import org.springframework.context.annotation.Configuration;

// This class can be extended for further logging configuration if needed.
@Configuration
public class LoggingConfig {
    // Currently, logging is configured via application.properties
    // Future structured logging frameworks (e.g., Logstash, ELK) could be integrated here.
}
